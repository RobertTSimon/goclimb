<div class="card-review" data-review-id="<%= review.id %>">
  <%= image_tag review.user.avatar_photo, class: "avatar-reviews" %>
  <p><%= review.description %></p>
  <% review.rating.times do %>
    <span>★</span>
  <% end %>
  <%= link_to "<i class='fas fa-trash-alt'></i>".html_safe, review_path(review), method: "delete", remote: true, data: { confirm: "Do you want to delete this review?"} %>
</div>

