<%= render partial: "shared/navBar" %>
<div class="full-height">
  <div class="form-card">
    <h1>Edit <%= @route.name %></h1>
      <%= simple_form_for @route do |f| %>
      <%= f.input :name, placeholder: 'Route Name' %>
      <%= f.input :description, placeholder: 'Description' %>
      <%= f.fields_for :photos do |p| %>
        <div class="field">
          <%= p.label :photo %><br>
          <%= p.file_field :photo, :multiple => true, name: "photos[photo][]" %>
          <%= p.hidden_field :photo_cache %>
        </div>
      <% end %>

      <%= f.input :latitude, placeholder: 'Latitude' %>
      <%= f.input :longitude, placeholder: 'Longitude' %>
      <%= f.input :type_of, placeholder: 'Type' %>
      <%= f.input :style, placeholder: 'Style' %>
      <%= f.input :level, placeholder: 'Level' %>

      <%= f.submit "Change", class: "btn btn-success" %>
    <% end %>
  </div>
</div>


